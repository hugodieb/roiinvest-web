'use client'

import RegisterForm from "@/app/_components/forms/auth/RegisterForm"

export default function Register() {

  return (
    <main className="bg-b flex items-center justify-center min-h-screen overflow-hidden">
      <RegisterForm />
    </main>
  )
}